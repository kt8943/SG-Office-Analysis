streamlit==1.58.0
pandas>=2.0
numpy>=1.24
altair>=5.0
duckdb>=1.0
xlrd>=2.0.1  # reads Data/Train Station Codes and Chinese Names.xls (load_mrt_stations)
requests>=2.28  # backend/geocode_buildings.py only (one-time script, not called by the app)
